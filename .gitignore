# macOS
.DS_Store
*.swp

# Xcode / build
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Generated by xcodegen — regenerate after clone with `xcodegen generate`.
# Keeping it out of git avoids leaking the developer's personal team ID
# and bundle identifier from Signing & Capabilities.
Breeves.xcodeproj/

# SPM
.build/
.swiftpm/
Package.resolved

# Secrets
Breeves/Secrets.plist
**/Secrets.plist
.env
.env.local

# Supabase
supabase/.branches
supabase/.temp

# Smoke-test outputs from prompt_smoke.ts — these are live API responses,
# not source. Regenerate by running the smoke script with ANTHROPIC_API_KEY.
supabase/functions/_shared/canned/_out/

# Bundle (handoff is a one-time export, keep out of repo)
Breeves\ iOS-handoff.zip
handoff/

# Editor
.idea/
.vscode/
